import React from "react";
import { Button } from "react-bootstrap";
const BootStarp = () => {
  return (
    <div>
      <h1>Bootstarp</h1>
      <Button onClick={() => alert("Hello")}>Click to alert show</Button>
    </div>
  );
};

export default BootStarp;
